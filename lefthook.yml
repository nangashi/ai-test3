colors: false

pre-commit:
  parallel: true
  commands:
    gitleaks:
      run: gitleaks git --staged --verbose --exit-code=1 --redact --no-banner --no-color
    terraform:
      run: just fix-tf {staged_files}
      glob: "*.tf"
      stage_fixed: true
